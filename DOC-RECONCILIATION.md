# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass. It treats the source code as read-only ground truth and edits only documentation so it reflects the repo's actual current state — no code was changed and no builds or tests were run. Code reality was established entirely by reading source files, manifests, and git history.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`

**Evidence basis:** verified-by-reading-code

`README.md:7` correctly describes the project as a monorepo of AI-powered tools: a meeting notes extractor, a triage-and-routing bot, a daily standup collector, a client portal, a Google Tasks integration, and a shared workspace package. All are confirmed present: `tools/meeting-notes/`, `slack-bots/triage-bot/`, `slack-bots/standup/`, `portal/`, `tools/meeting-notes/src/google-tasks.ts`, and `packages/shared/` — all with source files. No change needed.

---

### 2. Current state

**Status:** `drifted` → fixed

**Evidence basis:** verified-by-reading-code

All Phase 1 and Phase 2 deliverables are complete (confirmed by prior reconciliation at sha `6ba80ad`). The Portfolio Context phase checklists in `CLAUDE.md` correctly show all items as `[x]`. However, the `dc7aed2` commit (docs: lean CLAUDE.md via claude-md-lint) introduced a new "Build / Run" section to `CLAUDE.md` and omitted `cd slack-bots/standup && pnpm start` from it. The standup bot is fully implemented (`slack-bots/standup/package.json:7` — `"start": "node --import tsx src/index.ts"`) and the command was already present and correct in `README.md:41`. This same omission also existed in the legacy "How To Run" section preserved inside the Portfolio Context block.

Additionally, the `CLAUDE.md` architecture diagram (`CLAUDE.md:17–30`) did not list the `packages/shared/` workspace package, which exists at `packages/shared/src/` (6 source files: `anthropic-client.ts`, `slack-format.ts`, `types.ts`, `index.ts`, plus their tests) and is a declared dependency of every Slack bot and CLI tool (`@aiworkflow/shared: workspace:*` in each `package.json`). `README.md:17` correctly notes it as a feature; the architecture section was the gap.

**Changes to `CLAUDE.md`:**

1. `CLAUDE.md` architecture block — added `packages/shared/` entry:
   > Before: architecture block ends at `/tools/\n  meeting-notes/ ...`
   > After: added `/packages/\n  shared/               — @aiworkflow/shared: Anthropic client + Slack formatting utilities`

2. `CLAUDE.md` "Build / Run" section — added missing standup command:
   > Before: block ends at `cd slack-bots/triage-bot && pnpm start`
   > After: added `cd slack-bots/standup && pnpm start`

3. `CLAUDE.md` Portfolio Context "How To Run" section — same fix:
   > Before: block ends at `cd slack-bots/triage-bot && pnpm start`
   > After: added `cd slack-bots/standup && pnpm start`

---

### 3. Stack

**Status:** `consistent`

**Evidence basis:** verified-by-reading-code

`README.md` tech stack table and `CLAUDE.md` "Stack" section both accurately reflect the code:
- Next.js 14, React 18 — `portal/package.json:22,23` (`next: ^14.2.29`, `react: ^18.3.1`)
- Tailwind CSS, shadcn/ui — `portal/package.json:38` (`tailwindcss: ^3.4.17`), `portal/components.json` schema
- `@slack/bolt`, `@slack/web-api` — `slack-bots/standup/package.json:12,13`
- `@anthropic-ai/sdk` — `packages/shared/package.json:12`
- `googleapis` — `tools/meeting-notes/package.json:16`
- TypeScript 5.7, Zod, pnpm workspaces — root `package.json:16`, all `package.json` files
- Vitest, Testing Library — root `package.json:12`, `portal/package.json:33`

---

### 4. How to run

**Status:** `drifted` → fixed (see claim 2 above)

**Evidence basis:** verified-by-reading-code

`README.md` Quick Start section (`README.md:32–42`) is correct and includes all four runtime commands:
- `cd portal && pnpm dev` — confirmed by `portal/package.json:6`
- `cd tools/meeting-notes && pnpm extract ./notes.txt` — confirmed by `tools/meeting-notes/package.json:12`
- `cd tools/meeting-notes && pnpm start` — confirmed by `tools/meeting-notes/package.json:10`
- `cd slack-bots/triage-bot && pnpm start` — confirmed by `slack-bots/triage-bot/package.json:7`
- `cd slack-bots/standup && pnpm start` — confirmed by `slack-bots/standup/package.json:7`

`CLAUDE.md` was missing the standup command in two sections; both fixed under claim 2.

---

### 5. Known risks

**Status:** `consistent`

**Evidence basis:** verified-by-reading-code

The "Constraints" section in `CLAUDE.md` (no database, no server components, no stored credentials, Slack Bolt only) accurately reflects the code: `portal/src/lib/content.ts` reads from filesystem (no DB), all Slack bots use `@slack/bolt` as confirmed in each `package.json`. No contradictions found.

---

### 6. Next move

**Status:** `unverifiable`

**Evidence basis:** unverifiable-because-forward-looking

Phase 3 ("Content Engine + Scale") is operational content and marketing work (newsletter, LinkedIn, case studies, Beehiiv, referral program). There is no code to verify this against. `CLAUDE.md` Portfolio Context "Next Recommended Move" is advisory and cannot be confirmed or refuted by reading source files.

---

## Contradictions for Manual Review

**`RESUMPTION-PROMPT.md:43`** — States `claude-sonnet-4-5-20250929` as the Claude model. Actual code uses `claude-sonnet-4-6` (`packages/shared/src/anthropic-client.ts:55,76`).
One-line fix: change `claude-sonnet-4-5-20250929` → `claude-sonnet-4-6`.

**`RESUMPTION-PROMPT.md:14–27`** — "Current State" block says "Last completed phase: Phase 0", "What's Already Built: Nothing in code yet." All four phases of code are complete. This section is entirely stale.
One-line fix: update "Last completed phase: Phase 2 (Client Portal MVP — complete)" and remove the "What's NOT Built Yet" list, which now lists only completed work.

**`DISCOVERY-SUMMARY.md:60`** — Key Integrations table lists `claude-sonnet-4-5-20250929` as the Claude API model. Actual code uses `claude-sonnet-4-6` (`packages/shared/src/anthropic-client.ts:55,76`).
One-line fix: change `claude-sonnet-4-5-20250929` → `claude-sonnet-4-6`.

**`IMPLEMENTATION-ROADMAP.md:74`** — (Flagged in prior reconciliation, still unresolved.) Session 3 tasks reference `portal/content/clients/acme-corp/` but content lives at `content/clients/acme-corp/` (repo root, not under portal).
One-line fix: change `portal/content/clients/acme-corp/` → `content/clients/acme-corp/` in the Session 3 Tasks and Key Files blocks.

---

## Footer

**Reconciled:** 2026-06-02 19:29:52 PDT
**Branch:** `docs/truth-up-2026-06-02`
**HEAD sha:** `dc7aed226f4f8913bffd73f957ab11f2cea48d79`
