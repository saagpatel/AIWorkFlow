# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass. It treats the source code as read-only ground truth and edits only documentation so it reflects the repo's actual current state — no code was changed and no builds or tests were run. Code reality was established entirely by reading source files, manifests, and git history.

---

## Per-Claim Findings

### 1. What it is

**Status:** `drifted` → fixed

**Evidence basis:** verified-by-reading-code

README intro (`README.md:7`) stated "Two Claude-backed Slack bots handle the grunt work" but three tool packages exist: `slack-bots/triage-bot/`, `slack-bots/standup/`, and `tools/meeting-notes/`. All three are fully implemented.

**Change:** `README.md:7`
> Before: "Two Claude-backed Slack bots handle the grunt work: one extracts structured action items from raw meeting notes, another triages and routes incoming support tickets."
> After: "Three Claude-backed tools handle the grunt work: a meeting notes extractor, a triage-and-routing bot, and a daily standup collector."

---

### 2. Current state

**Status:** `drifted` → fixed

**Evidence basis:** verified-by-reading-code

Both `CLAUDE.md` sections ("Current Phase" at line 46 and "Current State" inside the Portfolio Context block at line 89) showed all Phase 1 and Phase 2 checklist items as unchecked `[ ]`. The code shows all deliverables are complete:

- **Phase 1 — Triage Bot:** `slack-bots/triage-bot/src/` has 7 source files (app.ts, classifier.ts, config.ts, prompts.ts, router.ts, index.ts + tests). `slack-bots/triage-bot/README.md` exists.
- **Phase 1 — Standup Collector:** `slack-bots/standup/src/` has 10 source files (app.ts, collectors.ts, config.ts, sheets.ts, store.ts, summarizer.ts, trigger-manual.ts, trigger-standup.ts, index.ts + tests). `slack-bots/standup/README.md` exists.
- **Phase 1 — Meeting Notes tool:** `tools/meeting-notes/src/` has 10 source files (app.ts, cli.ts, config.ts, extractor.ts, google-tasks.ts, index.ts, prompts.ts, slack-poster.ts + tests). `tools/meeting-notes/README.md` exists.
- **Phase 1 — Setup guides:** All three packages have `README.md` with setup instructions.
- **Phase 2 — Portal scaffolded:** `portal/` exists with full Next.js App Router structure, Tailwind, and shadcn/ui (`portal/components.json` confirms `"$schema": "https://ui.shadcn.com/schema.json"`).
- **Phase 2 — Dynamic routes:** All four routes exist: `portal/src/app/[client]/page.tsx`, `portal/src/app/[client]/audit/page.tsx`, `portal/src/app/[client]/automations/page.tsx`, `portal/src/app/[client]/metrics/page.tsx`. `generateStaticParams` is present in each.
- **Phase 2 — Password protection:** `portal/src/middleware.ts` implements cookie-based per-client auth with `/unlock` redirect.
- **Phase 2 — Deploy to Vercel:** `vercel.json` exists at repo root with correct build/output config. Recent git commits (`6ba80ad`, `f8d483d`) are titled "build(portal): add Vercel deployment config". Whether a custom domain is configured is a Vercel dashboard setting — unverifiable from code alone.

**Changes:** Both checklist blocks in `CLAUDE.md` updated — all 8 items changed from `[ ]` to `[x]`.

---

### 3. Stack

**Status:** `drifted` → fixed

**Evidence basis:** verified-by-reading-code

`README.md` tech stack table listed "Radix UI" for the portal layer. The portal uses **shadcn/ui**, not raw Radix UI directly. Evidence: `portal/components.json:1` contains `"$schema": "https://ui.shadcn.com/schema.json"`, and `CLAUDE.md:8` correctly states "UI: Tailwind CSS + shadcn/ui". Radix UI packages appear in `portal/package.json` as shadcn/ui's transitive dependencies, not as the primary component system.

**Change:** `README.md:46`
> Before: `| Portal | Next.js 14, React 18, Tailwind CSS, Radix UI |`
> After: `| Portal | Next.js 14, React 18, Tailwind CSS, shadcn/ui |`

---

### 4. How to run

**Status:** `drifted` → fixed

**Evidence basis:** verified-by-reading-code

`README.md` Quick Start documented three run commands but omitted the standup bot. All four commands are confirmed by reading `package.json` scripts:
- `portal/package.json` → `"dev": "next dev"` ✓
- `tools/meeting-notes/package.json` → `"extract": "tsx src/cli.ts"` ✓ and `"start": "node --import tsx src/index.ts"` ✓
- `slack-bots/triage-bot/package.json` → `"start": "node --import tsx src/index.ts"` ✓
- `slack-bots/standup/package.json` → `"start": "node --import tsx src/index.ts"` ✓ (was missing)

`README.md` also gained a standup bot entry in the Features section (see claim 1 fix).

**Change:** `README.md:39` — added `cd slack-bots/standup && pnpm start` under the Slack bots comment.

---

### 5. Known risks

**Status:** `consistent`

**Evidence basis:** verified-by-reading-code

The "Do NOT" constraints in `CLAUDE.md` (no database, no CRM, no raw HTTP for Slack, etc.) are project guidelines for future development, not factual claims about current state. No contradiction with implemented code was found: the portal reads from Markdown + JSON files with no database (`portal/src/lib/content.ts` uses `fs.readFileSync`), and all Slack bots use `@slack/bolt` (`@slack/bolt` is listed in all bot `package.json` dependencies).

---

### 6. Next move

**Status:** `consistent`

**Evidence basis:** verified-by-reading-code

With Phase 1 and Phase 2 now complete (per updated checklists), the documented next move aligns with `IMPLEMENTATION-ROADMAP.md` Phase 3: "Content Engine + Scale (Weeks 5-8) — NO CLAUDE CODE" — newsletter, LinkedIn, case studies, referral program. This is forward-looking operational work and cannot be falsified from code.

---

## Contradictions for Manual Review

**`IMPLEMENTATION-ROADMAP.md:74`** — Session 3 tasks reference content at `portal/content/clients/acme-corp/`, but the actual implementation reads content from the repo root: `portal/src/lib/content.ts:13` calls `path.join(process.cwd(), "..", "content", "clients")`, placing client data at `content/clients/` (not `portal/content/clients/`). The actual data lives at `content/clients/acme-corp/` and `content/clients/demo-co/`.

One-line fix: change `portal/content/clients/acme-corp/` → `content/clients/acme-corp/` in the Session 3 Tasks and Key Files blocks.

---

## Footer

**Reconciled:** 2026-05-30 20:08:14 PDT  
**Branch:** `docs/truth-up-2026-05-30`  
**HEAD sha:** `6ba80ad17ad82c63ed16986ac2ab9d5332e0b6ed`
